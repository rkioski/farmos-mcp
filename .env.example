# farmOS instance URL (no trailing slash)
FARMOS_URL=https://myfarm.example.com

# OAuth2 client credentials
# Create a client in farmOS: Admin > Config > Simple OAuth > Clients
FARMOS_CLIENT_ID=farm
FARMOS_CLIENT_SECRET=

# --- Password grant (recommended for personal use) ---
# Set both to use password grant flow
FARMOS_USERNAME=admin
FARMOS_PASSWORD=

# --- Client credentials flow ---
# Leave USERNAME and PASSWORD empty to use client credentials flow instead.
# The client must have the appropriate scopes configured in farmOS.

# Set to "false" to enable write tools (create_log, update_log)
# Defaults to "true" (read-only)
FARMOS_READ_ONLY=true

# Optional: set to DEBUG for verbose request/auth logging (default: WARNING)
# LOG_LEVEL=DEBUG
